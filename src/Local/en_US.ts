import { ILocal } from './index';

const localConfig: ILocal = {
  global: {
    prompt: 'Prompt',
    title: 'Title',
    ok: 'Ok',
    cancel: 'Cancel',
    null: 'No Data',
  },
};

export default localConfig;
